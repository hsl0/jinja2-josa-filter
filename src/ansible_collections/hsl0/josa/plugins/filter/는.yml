DOCUMENTATION:
  name: 는
  author: hsl0
  version_added: "0.1.0"
  short_description: Add the appropriate 은 or 는 particle
  description:
    - Appends the appropriate 은 or 는 particle to a Korean noun.
  positional: _input
  options:
    _input:
      description: The noun to which the particle is appended.
      type: str
      required: true
EXAMPLES: |
  - name: Add an 은 or 는 particle
    ansible.builtin.debug:
      msg: "{{ '사과' | hsl0.josa.는 }}"
RETURN:
  _value:
    description: The input noun with an 은 or 는 particle appended.
    type: str
