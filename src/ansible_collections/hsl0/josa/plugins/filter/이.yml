DOCUMENTATION:
  name: 이
  author: hsl0
  version_added: "0.1.0"
  short_description: 받침 있는 명사 뒤에만 이를 붙입니다
  description:
    - 받침이 있는 한국어 명사에는 이를 붙이고, 받침이 없는 명사는 그대로 반환합니다.
  positional: _input
  options:
    _input:
      description: 이를 조건부로 붙일 명사.
      type: str
      required: true
EXAMPLES: |
  - name: 필요할 때만 이 붙이기
    ansible.builtin.debug:
      msg: "{{ '지민' | hsl0.josa.이 }}"
RETURN:
  _value:
    description: 받침이 있을 때 이가 붙은 입력 명사.
    type: str
