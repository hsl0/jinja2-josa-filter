DOCUMENTATION:
  name: 다
  author: hsl0
  version_added: "0.1.0"
  short_description: 적절한 이다 또는 다 어미를 붙입니다
  description:
    - 한국어 명사에 적절한 이다 또는 다 어미를 붙입니다.
  positional: _input
  options:
    _input:
      description: 어미를 붙일 명사.
      type: str
      required: true
EXAMPLES: |
  - name: 이다 또는 다 어미 붙이기
    ansible.builtin.debug:
      msg: "{{ '철수' | hsl0.josa.다 }}"
RETURN:
  _value:
    description: 이다 또는 다 어미가 붙은 입력 명사.
    type: str
